openpyxl>=3.1
scikit-learn>=1.3
datasketch>=1.6
numpy>=1.24
beautifulsoup4>=4.12

# Optional: Russian lemmatization for title normalization (scripts/title_norm.py).
# Uncomment to enable — falls back to raw lowercased tokens when absent.
# pymorphy3>=2.0
